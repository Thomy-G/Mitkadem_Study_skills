# Past Exam Practice — 2025 Semester A, Moed A

Please write your answers in the designated areas and code blocks below.

---

## שאלה מספר 1 (25 נק')
*(השלימו את הקוד החסר עבור 12 המלבנים בקובץ השאלון)*

1.  `const express = require('express');`
2.  `const app = express();`
3.  
```js
   const express = require("express");
   var router = express.Router();
```
4.  
```js
.get(articleController.getArticles)
```
5.
```js
.post(aritcleController.creteArticle)
```
6.  
```js
route('/:id'")
```
7.  `________`
8.  `________`
9.  `________`
10. `________`
11. `________`
12. `________`

---

## שאלה מספר 2 (25 נק')

### א. אנדרואיד XML Layout
```xml
<!-- כתבו כאן את קוד ה-XML -->
```

### ב. עיצוב CSS וצבעי כפתורים
*   **צבע כפתור Foo (שמאלי):**
*   **צבע כפתור Bar (אמצעי):**
*   **צבע כפתור Foobar (ימני):**
*   **הסבר מפורט:**

---

## שאלה מספר 3 (20 נק')

### טבלת קבצי ה-Objects ב-Git
השלימו את פירוט האובייקטים (Commit, Tree, Blob) הקיימים בתיקייה `.git/objects`:

| סוג האובייקט | תוכן / תפקיד האובייקט |
| :--- | :--- |
| **Blob 1** | |
| **Blob 2** | |
| **Tree 1** | |
| **Tree 2** | |
| **Tree 3** | |
| **Commit 1** | |
| **Commit 2** | |

---

## שאלה מספר 4 (30 נק')

### א. פלט קוד ה-JavaScript
*   **הפלט המדויק:**
    ```
    
    ```
*   **הסבר מעקב ריצה:**

### ב. עקרון ה-OCP ב-SOLID (קוד ++C והסברים)
```cpp
// כתבו כאן קוד ++C מלא המדגים את OCP
```
*   **הסברים מתאימים:**
