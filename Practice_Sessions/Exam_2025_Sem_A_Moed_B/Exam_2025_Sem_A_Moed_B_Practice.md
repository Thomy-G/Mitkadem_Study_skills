# Past Exam Practice — 2025 Semester A, Moed B

Please write your answers in the designated areas and code blocks below.

---

## שאלה מספר 1 (25 נק')

### א. הסבר על HTTP Cookies ופעולתם
*   **מהן עוגיות (Cookies) ולמה הן משמשות:**
*   **כיצד הן עובדות בפועל:**

### ב. מימוש מנגנון התחברות (Login) מבוסס Cookies ב-Express
```javascript
// כתבו כאן את קוד השרת
```

---

## שאלה מספר 2 (25 נק')

### א. מימוש ממשק כרטיסיות ב-React / HTML
```jsx
// כתבו כאן את קוד ממשק הכרטיסיות
```

### ב. דיאגרמת Socket API מלאה של TCP
השלימו את הפונקציות המתאימות בשרת ובלקוח:

| שרת (Server) | לקוח (Client) |
| :--- | :--- |
| `socket()` | `socket()` |
| **[1]________** | None |
| **[2]________** | None |
| **[3]________** (blocks) | **[4]________** |
| `read()` / `write()` | `write()` / `read()` |
| **[5]________** | `close()` |

---

## שאלה מספר 3 (20 נק')

### הדגמת פיתוח TDD למערכת המלצת סרטים ב-++C
```cpp
// 1. ממשק ה-CLI ופונקציית ההמלצה (Header)

// 2. כתיבת 3 בדיקות יחידה ב-Google Test (gtest)

// 3. קוד המקור המינימלי שעובר את הבדיקות (.cpp)
```

---

## שאלה מספר 4 (30 נק')

### א. פלט קוד ה-Fork וה-Wait
*   **הפלט המדויק:**
    ```
    
    ```
*   **הסבר מעקב הריצה שלב אחרי שלב:**

### ב. אפליקציית אנדרואיד MVVM עם Repository ו-LiveData (Java)
```java
// 1. Repository
```
```java
// 2. ViewModel
```
```java
// 3. Activity (View)
```
